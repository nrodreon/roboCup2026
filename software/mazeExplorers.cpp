//
#include <parameters.hpp>
#include <Movement.cpp>

using namespace headerDefinition;

void setup(){}

void loop(){
  bool mazeFilled == false;
  bool junctionPresent == true;
  while (!mazeFilled){
    if (junctionPresent ||){
    } else{
      if ()
    }

  }  
}


std::array<bool,3> junctionUpdate(){
  std::array<bool,3> juncStatus;
  if (Sensor.distance() < minDistance){
    
  }
  Movement.left();

  Movement.right();
  Movement.right();

}
