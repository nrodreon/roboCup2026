#include <Sensor.hpp>
namespace Sensor{
  Colour colourIs(){
  /*
    if (reading == blueRange){
      return blue;
    } else if (reading == silverRange){
      return silver;
      } else if (reading ==blackRange ){
      return black;
      } else if (reading == whiteRange){
      return white;
      } else {
      return notExpected;
      }
    }

  */
  }
  
  double distance(){
  /*
    double distance;

    Sensor work which reads distance

    return distance;
   */
  }
}

